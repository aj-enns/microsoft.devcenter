# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Override the exclusion with negated pattern if you need to track specific .tfvars files
!terraform.tfvars.example

# Include override files you do wish to add to version control
#
# override.tf.json
# override_example.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Packer files
packer/variables.pkrvars.hcl
*.pkrvars.hcl
!*.pkrvars.hcl.example
packer_cache/
*.box